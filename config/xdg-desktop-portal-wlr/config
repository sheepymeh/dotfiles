[screencast]
exec_before=makoctl mode -a screencast; pkill -SIGRTMIN+4 i3blocks; pkill -SIGRTMIN+1 i3blocks
exec_after=grep webrtc-consume-stream <(pw-dump) || (makoctl mode -r screencast; pkill -SIGRTMIN+4 i3blocks; pkill -SIGRTMIN+1 i3blocks)
